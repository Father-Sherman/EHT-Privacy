title: "E-HT Weight Loss"
description: "Downloads and privacy policy for the E-HT Weight Loss app."
theme: jekyll-theme-cayman

# Keeps the Jekyll site index clean — only the two .md files in
# the repo root are pages; everything else (.gitignore, README,
# etc.) should not be served.
include:
  - privacy.md
  - index.md
  - downloads.md
